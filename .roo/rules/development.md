---
description: 
globs: 
alwaysApply: true
---
- Always use poetry to run python commands
- Use `poetry run pytest -v` to run all of the unit tests
- Use `poetry run flake8 .` to lint the code
- Use `poetry run black .` to format the code
- Use `poetry run pytest -v` to verify that the code cleanup did not break anything